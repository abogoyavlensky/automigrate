name: Release


on:
  workflow_run:
    workflows: Checks
    tags:
      - "*"
    types: completed
  push:
    tags:
      - "*"


jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v2

      - name: Build jar-file and deploy to Clojars
        run: docker-compose run --rm test bash -c "make deploy-ci"
